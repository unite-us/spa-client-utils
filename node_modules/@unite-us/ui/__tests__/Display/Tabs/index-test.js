import React from 'react';
import { shallow } from 'enzyme';
import Tabs from 'components/Display/Tabs';

describe('Tabs', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: 'id-1234',
      className: 'className-1234',
      value: 'myValue',
      onChange: jest.fn(),
    };
    component = shallow(<Tabs {...props} />);
  });

  it('renders', () => {
    expect(component.find('.ui-tabs')).toHaveLength(1);
  });
});
