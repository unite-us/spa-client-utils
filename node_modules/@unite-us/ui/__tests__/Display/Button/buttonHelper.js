import React from 'react';
import Icon from 'components/Display/Icon';

const defaultButtonProps = {
  label: 'Default Button',
};

const secondaryButtonProps = {
  label: 'Secondary Button',
  secondary: true,
};

const buttonWithTouchTapProps = {
  label: 'Touch Tap Button',
  onTouchTap: jest.fn(),
};

const iconButtonPropsLeft = {
  label: 'Icon Button',
  iconLeft: <Icon icon="IconCompass" color="red" />,
};

const iconButtonPropsRight = {
  label: 'Icon Button',
  iconRight: <Icon icon="IconCompass" color="red" />,
};

const noRenderButtonProps = {
  label: 'Should not work',
  shouldRender: false,
};

const fullWidthProps = {
  label: 'Full Width',
  fullWidth: true,
};

const linkProps = {
  label: 'link',
  link: true,
};


const buttonHelper = {
  defaultButtonProps,
  secondaryButtonProps,
  buttonWithTouchTapProps,
  iconButtonPropsLeft,
  iconButtonPropsRight,
  noRenderButtonProps,
  fullWidthProps,
  linkProps,
};

export default buttonHelper;
