import React from 'react';
import { shallow } from 'enzyme';
import TableHeader from 'components/Display/Table/components/TableHeader';

describe('Table', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: '1234',
      className: 'ui-table-header',
      onCellClick: jest.fn(),
      secondaryDataList: [],
      style: {},
      children: [],
    };
    component = shallow(<TableHeader {...props} />);
  });

  it('renders the table header', () => {
    expect(component.find('.ui-table-header')).toHaveLength(1);
  });
});
