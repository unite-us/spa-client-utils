import React from 'react';
import { shallow } from 'enzyme';
import TableBody from 'components/Display/Table/components/TableBody';

describe('Table', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: '1234',
      className: 'ui-table-body',
      onCellClick: jest.fn(),
      secondaryDataList: [],
      style: {},
      children: [],
    };
    component = shallow(<TableBody {...props} />);
  });

  it('renders the table body', () => {
    expect(component.find('.ui-table-body')).toHaveLength(1);
  });
});
