import React from 'react';
import { shallow } from 'enzyme';
import TableRow from 'components/Display/Table/components/TableRow';

describe('TableRow', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: '1234',
      className: 'ui-table-row',
      clickable: true,
      children: [],
      onCellClick: jest.fn(),
      rowIndex: 0,
      parent: null,
      secondaryDataList: [],
      style: {},
    };
    component = shallow(<TableRow {...props} />);
  });

  it('renders the table row', () => {
    expect(component.find('.ui-table-row')).toHaveLength(1);
  });
});
