import React from 'react';
import { shallow } from 'enzyme';
import TableHeaderColumn from 'components/Display/Table/components/TableHeaderColumn';

describe('Table', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: '1234',
      className: 'ui-table-header-column',
      columnIndex: 0,
      style: {},
      children: '',
    };
    component = shallow(<TableHeaderColumn {...props} />);
  });

  it('renders the table header column', () => {
    expect(component.find('.ui-table-header-column')).toHaveLength(1);
  });
});
