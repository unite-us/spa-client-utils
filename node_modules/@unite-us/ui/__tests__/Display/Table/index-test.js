import React from 'react';
import { shallow } from 'enzyme';
import Table from 'components/Display/Table';

describe('Table', () => {
  let props;
  let component;

  beforeAll(() => {
    props = {
      id: '1234',
      className: 'ui-table',
      onCellClick: jest.fn(),
      secondaryDataList: [],
      style: {},
      children: [],
    };
    component = shallow(<Table {...props} />);
  });

  it('renders the table', () => {
    expect(component.find('.ui-table')).toHaveLength(1);
  });
});
