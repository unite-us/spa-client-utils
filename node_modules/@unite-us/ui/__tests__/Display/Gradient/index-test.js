import React from 'react';
import { shallow } from 'enzyme';
import Gradient from 'components/Display/Gradient';

describe('Gradient', () => {
  it('renders', () => {
    const wrapper = shallow(<Gradient><div>Hey Pete!</div></Gradient>);

    expect(wrapper.find('.ui-gradient')).toHaveLength(1);
  });
});
